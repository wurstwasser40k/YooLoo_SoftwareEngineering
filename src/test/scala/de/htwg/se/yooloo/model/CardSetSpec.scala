package de.htwg.se.yooloo.model

import org.scalatest.{ Matchers, WordSpec }

/**
 * Created by svenb on 11.04.2017.
 */
class CardSetSpec extends WordSpec with Matchers {
  //TODO: Klären, ob Tests je Element verschachteln

  /*
  "Calling addToCardSet(1)" should {
    "create a List(1)" in {
      val set1 = CardSet()
      set1.addToCardset(1)
      assert(set1.cardSet == List(1))

    }
  }

  "Calling addToCardSet(1) addToCardSet(2)" should {
    "create a List(2,1)" in {
      val set1 = CardSet()
      set1.addToCardset(1)
      set1.addToCardset(2)
      assert(set1.cardSet == List(2, 1))
    }
  }

  "Calling addToCardSet(1) addToCardSet(15) " should {
    "create a List(1)..because 15 shall not be added to the List" in {
      val set1 = CardSet()
      set1.addToCardset(1)
      set1.addToCardset(15)

      assert(set1.cardSet == List(1))
    }
  }

  "Calling addToCardSet(1) addToCardSet(0) " should {
    "create a List(1)..because 0 shall not be added to the List" in {
      val set1 = CardSet()
      set1.addToCardset(1)
      set1.addToCardset(0)

      assert(set1.cardSet == List(1))
    }
  }

  "Calling addToCardSet(1) addToCardSet(1) " should {
    "create a List(1)..because no duplicates are allowed " in {
      val set1 = CardSet()
      set1.addToCardset(1)
      set1.addToCardset(1)

      assert(set1.cardSet == List(1))
    }
  }
*/
}
